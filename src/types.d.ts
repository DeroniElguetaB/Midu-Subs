// en este archivo .d.ts solo pueden ir definiciones, interfaces, types, etc...
interface Sub{
    nick: string,
    subMonths: number,
    avatar: string,
    description?: string
}

export default Sub